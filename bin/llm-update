#!/bin/zsh

bin=$(dirname $(readlink -f $0))
parent=$(dirname $bin)

source $parent/config/config.sh

if [ -z "$ENABLE_OPEN_WEBUI" ]; then
  echo "Open-webui is not enabled"
  exit 1
fi

echo "🔄 Updating LLM Stack..."
docker-compose pull
docker-compose up -d
echo "✅ Stack updated!"
